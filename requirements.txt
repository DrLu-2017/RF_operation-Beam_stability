# Core dependencies for ALBuMS Streamlit Application
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0
scipy>=1.11.0
numpy>=1.24.0
matplotlib>=3.8.0
h5py>=3.10.0
mpmath>=1.3.0
seaborn>=0.13.0

# ALBuMS and related dependencies
# Note: 'sh' module is NOT included as it only works on Linux/macOS
# On Windows, ALBuMS will work without shell command execution


paramiko>=3.0.0
